export const MAX_IMAGE_WIDTH = 400; // Further reduced for better storage management
export const JPEG_QUALITY = 0.4; // Further reduced for better compression
export const MAX_FILE_SIZE = 2 * 1024 * 1024; // 2MB limit